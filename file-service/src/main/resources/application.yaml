server:
  port: 8083
  servlet:
    context-path: /file

spring:
  application:
    name: file-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/file-service?authSource=admin
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

app:
  file:
#    for window
#    storage-dir: C:/upload
#    for mac/linux
    storage-dir: /Users/thangnguyenthien/Documents/Upload
    download-prefix: http://localhost:8888/api/v1/file/media/download/